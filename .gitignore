node_modules
dist
dist-ssr
.vercel
.env
.env.local
*.local
.DS_Store
npm-debug.log*
.env*

# Vercel appends a broad .env* rule above; the template must still be tracked.
!.env.example

# Pitch-video footage: large, and reproducible with scripts/record.mjs.
# The script and docs/VIDEO.md are tracked; the .webm files are not.
docs/footage/

# Local test imagery for the Street Audit. Not committed: these are Google
# Street View captures, and the repo is public.
test-photos/
